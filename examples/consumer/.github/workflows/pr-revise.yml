name: pr-revise
# Dispatched by the review workflow when the review bot requests changes on an
# auto-agent PR. The file MUST be named pr-revise.yml (revise_dispatch.sh
# dispatches that workflow name) unless you set AGENT_OPS_REVISE_WORKFLOW.

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: PR number to revise
        required: true
        type: number

permissions:
  contents: write
  pull-requests: write

jobs:
  revise:
    uses: Netis/agent-ops/.github/workflows/revise.yml@v0.2.0
    with:
      pr_number: ${{ inputs.pr_number }}
      agent_ops_ref: v0.2.0
      runner_labels: '["self-hosted","my-runner"]'
    secrets: inherit
